let addTwo:(x:number,y:number) => number = function(x:number,y:number){
    return x + y
}

//可选参数 默认参数 可选参数必须放在最后

// function sum(x:string,z='kaka',y?:string): string{
//     if(y){
//         return x + ' ' + y
//     }
//     return x
// }

